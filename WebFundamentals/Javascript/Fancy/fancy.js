var myArray = ['James','Jill','Jane','Jack'];

for(var index in myArray) {
    console.log( index + " : " + myArray[index]);
  } console.log('');
  
  for(var index in myArray) {
    console.log( index + " -> " + myArray[index]);
  } console.log('');
  
  for(var index in myArray) {
    console.log( index + " => " + myArray[index]);
  } console.log('');
  
  for(var index in myArray) {
    console.log( index + " :: " + myArray[index]);
  } console.log('');
  
  for(var index in myArray) {
    console.log( index + " ----- " + myArray[index]);
  }
  

